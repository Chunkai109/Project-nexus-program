# Copy this file to .env and fill in your Supabase project's values
# (Dashboard → Settings → API). The anon key is the public one — safe
# to embed in client code — never put the service_role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
